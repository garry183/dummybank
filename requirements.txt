# Dummy Bank Application Requirements

# Core dependencies for CLI version (Python standard library only):
# - json (for data persistence)
# - os (for file system operations)
# - hashlib (for password hashing)
# - datetime (for timestamps)
# - tempfile (for testing)
# - unittest (for test suite)

# Web interface dependencies (Flask):
Flask>=2.0.0
Werkzeug>=2.0.0

# Python 3.6+ required